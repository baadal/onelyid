{
  "name": "onelyid",
  "version": "0.0.0",
  "description": "onelyid monorepo",
  "author": "abraj",
  "license": "MIT",
  "private": true,
  "main": "index.js",
  "scripts": {
    "tsc": "pnpm -r run tsc",
    "build": "pnpm -r build",
    "build:server": "pnpm -r run build:server",
    "pack": "pnpm run build && rm -f artifacts/*.tgz && pnpm -r run pack:dest",
    "publish:test": "pnpm -r publish --dry-run",
    "publish": "pnpm -r publish",
    "clean": "rm -rf node_modules tsconfig.tsbuildinfo && pnpm -r run clean"
  },
  "type": "commonjs",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/baadal/onelyid.git"
  },
  "keywords": [
    "onelyid",
    "atproto",
    "login",
    "auth",
    "authentication",
    "bluesky",
    "oauth",
    "oauth2",
    "server",
    "node",
    "backend"
  ],
  "devDependencies": {
    "@changesets/cli": "^2.29.8",
    "@types/node": "^25.0.3",
    "tsup": "^8.5.1",
    "typescript": "^5.9.3"
  }
}
